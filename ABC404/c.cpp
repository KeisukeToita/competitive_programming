#include <bits/stdc++.h>

#define REP(i, n) for(int i = 0; i < n; i++)
#define REPR(i, n) for(int i = n; i >= 0; i--)
#define FOR(i, m, n) for(int i = m; i < n; i++)

#define INF 2e9

#define ALL(v) v.begin(), v.end()

using namespace std;
typedef long long ll;

int main(void){
    int N, M;
    cin >> N >> M;
    if(N != M){
        cout << "No" << endl;
        return 0;
    }

    vector<int> a(N, 0);
    vector<int> b(N, 0);
    int Ai, Bi;
    for(int i = 0; i < M; i++){1] == 0){
                cout << "No" << endl;
                return 0;
            }else{
        c[a[point] - 1]++;
    }
    
    cout << "Yes" << endl;
    return 0;
}